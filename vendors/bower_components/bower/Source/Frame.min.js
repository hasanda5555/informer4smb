(function(){var previous,started=false,callbacks=[],remove=[],frameTime=16,requestAnimationFrame=function(callback){window.setTimeout(callback,frameTime)};function startAnimation(){if(!started){previous=Date.now();requestAnimationFrame(frame);started=true}}function invokeFrame(){var fn,i,l,now=Date.now(),delta=Math.min(now-previous,1e3);for(i=0,l=remove.length;i<l;i++){coreEraseOne(callbacks,remove[i])}remove.length=0;for(i=0,l=callbacks.length;i<l;i++){fn=callbacks[i];if(remove.indexOf(fn)==-1){fn.call(null,delta)}}previous=now}function frame(){requestAnimationFrame(frame);if(callbacks.length==0){remove.length=0;previous=Date.now()}else invokeFrame()}atom.frame={add:function(fn){startAnimation();includeUnique(callbacks,fn)},remove:function(fn){if(started)includeUnique(remove,fn)}}})();